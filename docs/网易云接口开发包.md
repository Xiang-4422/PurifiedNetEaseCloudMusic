# 网易云接口开发包

## 1. 目标

把 `NeteaseCloudMusicApiEnhanced/api-enhanced` 复刻为可长期维护的 Dart SDK，并支持后续跟随上游刷新。

SDK 层只负责：

```text
接收 query -> 构造网易云内部请求 -> 选择加密方式 -> 发送请求 -> 返回原始响应
```

不负责 App 缓存、播放队列、UI、本地数据库和领域实体。

## 2. 上游来源

- 上游仓库：https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced
- 本地 submodule：`third_party/api-enhanced`
- Dart SDK：`packages/netease_music_api`

当前同步基线：

```text
版本：v4.35.2
commit：60ee927e3224ab675679d023065225af11bdc693
module 数量：399
确认日期：2026-06-20
```

## 3. 刷新流程

```bash
git submodule update --remote third_party/api-enhanced
git diff --submodule

node packages/netease_music_api/tool/generate_api_enhanced_modules.js
node packages/netease_music_api/tool/generate_api_enhanced_modules.js --check

flutter test packages/netease_music_api/test/api_enhanced_raw_test.dart
flutter test test/data/music_data/sources/netease/api_upstream_sync_test.dart
flutter test test/architecture
flutter analyze
```

首次拉取包含 submodule 的仓库后执行：

```bash
git submodule update --init --recursive
```

## 4. 生成器

生成器：

```text
packages/netease_music_api/tool/generate_api_enhanced_modules.js
```

生成产物：

```text
packages/netease_music_api/lib/src/generated/api_enhanced_modules.g.dart
packages/netease_music_api/lib/src/endpoints/raw/api_enhanced_raw_methods.g.dart
```

当前规则：

- 覆盖上游 `module/*.js`。
- 无 request path 的模块标为 `special`。
- 暂不支持的 crypto 标为 `special`。
- 生成器会格式化 Dart，并且只在内容变化时写回。
- `--check` 模式只校验生成产物是否最新，不写文件；产物落后时退出失败并提示重新生成。

自动支持的 crypto：

```text
weapi
eapi
linuxapi
api
xeapi
```

`xeapi` 需要先完成 `register_xeapikey`，SDK 会持久化公钥状态并在请求响应后更新 xeapi session。

## 5. 当前 special 模块

```text
api
audio_match
avatar_upload
cloud
cloud_import
cloud_upload_complete
cloud_upload_token
decrypt
eapi_decrypt
inner_version
login_qr_create
playlist_track_all
playlist_tracks
playlist_cover_update
register_anonimous
register_xeapikey
related_playlist
scrobble
song_url_match
song_url_ncmget
song_url_v1
song_url_v1_302
top_list
vip_sign_history
vip_tasks_v1
voice_upload
```

## 6. 当前差异

已实现或已有手写入口：

- `api`
- `audio_match` 的固定参数音频指纹匹配和上游返回 envelope
- `avatar_upload` 的图片 token 分配、二进制上传和头像更新 envelope
- `cloud`
- `cloud_import` 的上传检查与导入两段请求
- `cloud_upload_token` 的上传检查、NOS token、LBS 地址和上游 envelope
- `cloud_upload_complete` 的云盘信息提交、发布请求、上游 envelope 和错误分支
- `eapi_decrypt` 的 `hexString` 请求/响应解密
- `decrypt` 的 `eapi`、`weapi` 响应、`linuxapi`、`xeapi` 响应和 `api` 分支
- `inner_version` 的上游 package version 返回
- `login_qr_create` 的上游返回 envelope、`platform=web` chainId 和 `qrimg` PNG data URL
- `playlist_track_all` 的歌单详情与歌曲详情两段请求
- `playlist_tracks` 的 512 fallback 重试
- `playlist_cover_update` 的图片上传、封面更新 envelope 和缺图校验
- `register_xeapikey`
- `register_anonimous`
- `related_playlist` 的歌单页 HTML 抓取与相关歌单解析
- `scrobble` 的 startplay/play 双 weblog 上报
- `song_url_v1` 的 xeapi 基础请求
- `song_url_v1_302` 的下载 URL 与播放 URL fallback
- `song_url_match` 的 unblock 依赖限制响应和上游返回 envelope
- `song_url_ncmget` 的上游等价空响应 envelope
- `top_list` 的 `idx` 错误分支和按 `id` 请求榜单详情
- `vip_sign_history`
- `vip_tasks_v1`
- `voice_upload` 的 NOS multipart 上传、preCheck、发布请求和上游 envelope

运行时 option 当前状态：

- `realIP` 会写入 `X-Real-IP` 和 `X-Forwarded-For`。
- `randomCNIP` 会在 Dart raw metadata 阶段生成请求 IP，显式 `realIP` 优先。
- `ua` 会映射为原始 User-Agent。
- `checkToken` 会写入 eapi header。
- `e_r` 会进入 raw request options；eapi/weapi 请求开启后按加密响应读取并解密。
- `proxy` 支持 HTTP/HTTPS 代理 URL，并在 Dart native `HttpClient` 层生效。
- `domain`、`cookie` 已进入 request options。
- 已建立 Node oracle fixture，覆盖专辑详情/动态/订阅/新碟/数字专辑列表与榜单、`album_privilege`、搜索/云搜索/默认词/热词/详情/匹配/多类型/建议、歌曲详情/URL/音质/动态封面/版权推荐/创作者/喜欢/排序/百科/副歌/红心数、歌词、云盘歌词/匹配/下载、下载记录、已购单曲、歌词摘录、`login`、`login_cellphone`、登录二维码、短信验证码、国家区号、风控二维码、账号状态/刷新/退出、喜欢列表、签到、推荐资源、私人 FM、FM 垃圾桶、banner、首页入口、首页 block、个性化新歌/MV/电台/独家放送、推荐电台节目、`playlist_detail`、`playlist_detail_dynamic`、`playlist_detail_rcmd_get`、`playlist_video_recent`、`playlist_hot`、`top_playlist`、`top_playlist_highquality`、`playlist_subscribe`、歌单创建/删除/编辑/导入/订阅者/分类、`playmode_intelligence_list`、`playmode_song_vector`、`recent_listen_list`、一起听房间/心跳/播放指令/同步列表、`user_playlist`、用户账号/等级/收藏计数/云盘/听歌排行/关注粉丝/电台/动态/评论历史、用户手机号绑定/换绑/资料/徽章/社交状态、`comment`、`comment_music`、`comment_playlist`、`comment_album`、`comment_dj`、`comment_event`、`comment_mv`、`comment_video`、`comment_hot`、`comment_new`、`comment_like`、`comment_floor`、`comment_report`、`hug_comment`、`comment_hug_list`、`comment_info_list`、`like`、`personalized`、推荐歌曲、不感兴趣、历史与相似推荐、`voicelist_search`、新歌速递、新碟、数字专辑详情/购买/已购/销量、热门歌手、歌手详情、歌手介绍、歌手专辑、歌手 MV、歌手热门歌曲、歌手单曲、歌手列表、歌手粉丝/新作品/订阅/视频、UGC 内容纠错/贡献、用户详情、视频/MV、MV 列表/详情/订阅、视频列表/详情/订阅/时间线、最近播放、下载 URL、音乐可用性、红心检查、关注列表、榜单、榜单维度详情/歌曲列表、电台节目、DJ 电台分类/详情/DIFM/订阅/榜单、广播电台、助眠解压、跑步漫游、听歌足迹、VIP 信息与任务、云贝等模块，并覆盖 `api`、`linuxapi`、`xeapi` 显式 crypto 分支，用于对比上游模块传给 `request` 的 URI、data 和 options。
- Node oracle 已扩展到 `song_url_v1`、`vip_sign_history`、`vip_tasks_v1` 这三个 xeapi special module，固定 Dart 手写入口与上游 Node 的 URI、data 和 options 一致。
- Node oracle 已支持多请求 special module 序列对照，并覆盖 `song_url_v1_302` 的下载/播放 URL fallback、`playlist_track_all` 的歌单详情/歌曲详情、`scrobble` 的 startplay/play weblog、`cloud_import` 的上传检查/导入请求、`avatar_upload`、`playlist_cover_update`、`cloud_upload_token`、`cloud_upload_complete`、`voice_upload` 的上传请求序列、`playlist_tracks` 的 512 retry。
- `login_qr_create` 已用 Dart 本地测试固定 PC 默认 URL、Web chainId 和二维码图片 data URL。
- `related_playlist` 已用 Dart 本地 HTML fixture 固定封面、歌单 ID、歌单名和创建者解析。
- `inner_version` 已由生成器读取上游 package.json 版本，避免 SDK 手写版本落后于 submodule。
- `audio_match` 已用 Dart 本地 HTTP fixture 固定上游固定 query 和 `status/body` 返回结构。
- 解灰相关 special module 已固定 raw 返回 envelope：`song_url_v1` unblock 限制、`song_url_match` 限制和 `song_url_ncmget` 空响应。
- 图片上传 special module 已固定 raw 返回 envelope：`avatar_upload` 成功路径、`playlist_cover_update` 成功路径和缺图 400 分支。
- 云盘上传拆分模块已固定 raw 返回 envelope：`cloud_upload_token` 的缺参/成功分支、`cloud_upload_complete` 的缺参/信息上传失败/成功分支。
- `voice_upload` 已固定 raw multipart 上传链路：缺文件 500、NOS initiate/part/complete、preCheck、upload/v2 和 `status/body` 返回结构。
- `login`、`login_cellphone`、`login_qr_key`、`login_qr_check`、短信验证码、国家区号和风控二维码已固定 raw request data 字段变换，避免账号入口把上游 query 字段直接透传。
- `mv_all`、`mv_detail`、`mv_detail_info`、`mv_exclusive_rcmd`、`mv_first`、`mv_sub`、`mv_sublist` 已固定 raw request data 和订阅动态 path。
- `video_category_list`、`video_detail`、`video_detail_info`、`video_group`、`video_group_list`、`video_sub`、`video_timeline_all`、`video_timeline_recommend` 已固定 raw request data 和订阅动态 path。
- `artist_fans`、`artist_follow_count`、`artist_new_mv`、`artist_new_song`、`artist_sub`、`artist_sublist`、`artist_video` 已固定 raw request data 和订阅动态 path。
- `ugc_album_get`、`ugc_artist_get`、`ugc_artist_search`、`ugc_detail`、`ugc_mv_get`、`ugc_song_get`、`ugc_user_devote` 已固定 raw request data 字段变换。
- `user_bindingcellphone`、`user_medal`、`user_mutualfollow_get`、`user_replacephone`、`user_social_status`、`user_social_status_edit`、`user_social_status_rcmd`、`user_social_status_support`、`user_update` 已固定 raw request data 字段变换。
- `djRadio_top`、`dj_banner`、`dj_category_excludehot`、`dj_category_recommend`、`dj_catelist`、`dj_detail`、DIFM 频道、`dj_hot`、`dj_paygift`、`dj_personalize_recommend`、节目榜、分类推荐、订阅列表、订阅者、今日优选和付费榜已固定 raw request data 与订阅动态 path。
- `listentogether_accept`、`listentogether_end`、`listentogether_heatbeat`、`listentogether_play_command`、`listentogether_room_check`、`listentogether_room_create`、`listentogether_status`、`listentogether_sync_list_command`、`listentogether_sync_playlist_get` 已固定 raw request data 和播放/歌单同步 JSON 字段。
- App 正在使用的 typed API 必须用同步测试固定 metadata；当前已覆盖歌单操作、歌曲详情、歌词、红心、用户歌单和播放 URL，避免便利层落后于 raw API。

仍未完整复刻：

- `song_url_v1` 的 unblock 分支依赖 `unblockmusic-utils`。
- `song_url_match` 依赖 `unblockmusic-utils`。
- `decrypt` 仍不支持 `weapi` 请求解密和 `xeapi` 请求解密；这两个分支上游也明确返回限制说明。
- `proxy` 暂不支持 PAC 和带认证的代理。
- `song_url_v1` 普通请求固定 `encodeType=flac` 并已对齐上游；`source` 顺序只属于 unblockmusic-utils 解灰分支，Dart SDK 不伪造替代链路，App 继续使用播放失败后的刷新和降级策略兜底。
- Node oracle 仍需继续扩大到更多 normal module 和 special module。

## 7. 测试要求

raw manifest 测试必须覆盖：

- manifest 覆盖所有上游 module。
- generated method name 唯一。
- normal module 能构建 request metadata。
- normal module 有 path。
- normal module crypto 受支持。
- special module 清单显式固定。
- `xeapi` module 必须 special。
- pathless module 必须 special。
- unsupported crypto 不能静默 fallback。
- `randomCNIP` 必须生成可用 IPv4，且不能覆盖显式 `realIP`。
- `proxy` 必须能映射为 Dart native proxy rule，不支持的代理形式必须显式报错。
- Node oracle fixture 必须验证 Dart metadata 与上游 Node request metadata 一致。

当前验证结果：

```text
raw manifest tests: 覆盖 manifest、special module、crypto、runtime option
architecture tests: 必须通过
full test suite: 必须通过
flutter analyze: No issues found
git diff --check: passed
```

## 8. 后续 goal

1. 扩展上游 Node oracle fixture 覆盖范围。
2. 继续扩大 `decrypt` 与 `eapi_decrypt` 的 oracle 覆盖。
3. 评估 `song_url_match`、`song_url_ncmget` 和 unblock 替代方案。
4. 评估 PAC 和带认证代理是否需要补齐。
5. 评估 unblockmusic-utils 的 Dart 替代方案；在替代方案明确前，source order 只作为不支持的解灰能力记录。
6. 每次上游刷新后更新差异清单，直到所有 special module 都有实现、替代或限制说明。
