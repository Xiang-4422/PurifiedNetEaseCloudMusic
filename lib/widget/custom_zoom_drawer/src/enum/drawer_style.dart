/// 抽屉内置动画样式。
enum DrawerStyle {
  /// 默认的缩放、位移和可选阴影样式。
  defaultStyle,

  /// 菜单从侧边平移覆盖的样式。
  style1,

  /// 主内容缩放并沿对角线移动的样式。
  style2,

  /// 主内容带透视旋转展开的样式。
  style3,

  /// 主内容反向透视旋转展开的样式。
  style4,
}
